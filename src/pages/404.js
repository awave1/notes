import React from 'react';

function NotFound() {
  return (
    <small
      style={{
        margin: '0 auto',
        alignSelf: 'center',
        fontFamily: 'Hack, monospace',
      }}
    >
      emptyspace
    </small>
  );
}

export default NotFound;
